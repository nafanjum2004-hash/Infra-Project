---
- name: Create systemd service file
  template:
    src: employee-management.service.j2
    dest: /etc/systemd/system/employee-management.service
    mode: '0644'

- name: Reload systemd
  systemd:
    daemon_reload: true